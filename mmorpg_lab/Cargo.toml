[workspace]
resolver = "2"
members = [
    "client",
    "dedicated_server",
    "orchestrator",
    "gatekeeper",
    "shared"
, "game_sockets"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
