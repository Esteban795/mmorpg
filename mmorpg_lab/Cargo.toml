[workspace]
resolver = "2"
members = [
    "client",
    "dedicated_server",
    "orchestrator",
    "gatekeeper",
    "shared"
, "spatialserver"
, "game_sockets", "fakebroker"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
